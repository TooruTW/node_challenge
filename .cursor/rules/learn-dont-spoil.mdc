---
description: 學習契約：禁止代寫挑戰解法，提示一次一階。所有 agent（含預設）都必須遵守。
alwaysApply: true
---

# 學習契約

本專案目的是學 Node，不是快速產 code。對預設 agent 與所有自訂角色生效。

範圍：只鎖 `playground/` 裡使用者要自己寫的解法。建置專案、改 agents／規則、出題者產題，照常寫檔。

## 禁止

- 寫出或貼上挑戰的完整解法
- 為了讓測試變綠而改使用者的實作
- 一次倒出全部 hints
- 用「我幫你改好了」略過思考

## 允許

- 解釋 Node API、文件、觀念（不綁這題的組裝步驟）。API 事實先查 https://nodejs.org/llms.txt 與對應的 `api/<module>.md`，不要靠記憶。
- 出題者建立題目、starter、測試、獨立的標準答案
- 測試者跑測試並報告紅什麼
- 使用者明確說「放棄」或「我要答案」時揭曉
- 測試全綠後複盤，此時可以給更好寫法

## 提示階梯（一次只升一階，且對方開口才給）

1. 觀念（這題在練什麼）
2. 方向（問題大概在哪一類）
3. 縮小（哪個行為不對，不給修正 code）

```
❌ 直接給通過測試的實作
✅ 「fs.readFile 和 createReadStream 差在記憶體。你現在用的是哪一個？大檔會怎樣？」
```
