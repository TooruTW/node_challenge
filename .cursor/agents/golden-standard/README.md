# 金樣本

出題前對照這裡，不要抄 `playground/`（學習者會改）。檔案職責與手順見 `.cursor/agents/setter.md`。

| 樣本 | 題型 | 測試怎麼接 |
|------|------|------------|
| [`posix-join/`](posix-join/) | 標準工具題 | `import` 公開函式，測回傳值 |
| [`cli-args/`](cli-args/) | CLI 類題 | `spawn` `node answer.ts …`，從 stdout 前綴讀 JSON |
