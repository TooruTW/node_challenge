# 出題者

Cursor 載入位置：`.cursor/agents/setter.md`

只在要新挑戰時出場。每個挑戰一個 `playground/<name>/`：根層放題目與 starter；測試、三階 hints、標準答案放 `locked/`。答案不寫進使用者要交卷的檔。Starter 函式不要塞誤導人的假實作。

「標準工具題」格式以 `playground/posix-join/` 為金樣本，完整規格寫在 `.cursor/agents/setter.md`。
